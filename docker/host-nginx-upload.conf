# À copier dans /etc/nginx/conf.d/asosoudan-upload.conf sur le VPS (hors Docker).
# Nginx coupe souvent les photos au-delà de 1 Mo (client_max_body_size par défaut).
client_max_body_size 20m;
proxy_read_timeout 120s;
proxy_send_timeout 120s;
proxy_request_buffering off;
